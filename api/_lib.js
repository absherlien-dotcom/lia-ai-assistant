import crypto from "crypto";export const LIA_USER=process.env.LIA_USER||"hesham1amd";export const LIA_PASS=process.env.LIA_PASS||"1236542080";export const LIA_SECRET=process.env.LIA_SESSION_SECRET||crypto.createHash("sha256").update(`${LIA_USER}:${LIA_PASS}:lia-private-v4`).digest("hex");export function parseCookies(c=""){return Object.fromEntries(c.split(";").map(p=>p.trim()).filter(Boolean).map(p=>{const i=p.indexOf("=");return i===-1?[p,""]:[p.slice(0,i),decodeURIComponent(p.slice(i+1))]}))}export function makeToken(){return crypto.createHash("sha256").update(`${LIA_USER}:${LIA_PASS}:${LIA_SECRET}`).digest("hex")}export function isLoggedIn(req){const cookies=parseCookies(req.headers.cookie||"");return cookies.lia_session===makeToken()}export function requireAuth(req,res){if(!isLoggedIn(req)){res.status(401).json({error:"UNAUTHORIZED",reply:"يا هشام، تحتاج تسجيل الدخول أولًا."});return false}return true}export function setSessionCookie(res){const secure=process.env.NODE_ENV==="production"?"; Secure":"";res.setHeader("Set-Cookie",`lia_session=${makeToken()}; HttpOnly; SameSite=Lax; Path=/; Max-Age=31536000${secure}`)}export function clearSessionCookie(res){res.setHeader("Set-Cookie","lia_session=; HttpOnly; SameSite=Lax; Path=/; Max-Age=0")}export const LIA_PERSONALITY=`أنتِ ليا، مساعد ذكاء اصطناعي شخصي خاص بهشام فقط. أنتِ عقل شخصي ثاني لهشام: ذكية، هادئة، ودودة، عملية، وتتكلمين بلهجة يمنية رسمية خفيفة عند الحاجة. إذا طلب هشام تغيير اللهجة أو الأسلوب، غيّريه فورًا. مهمتك تنظيم حياة هشام اليومية، أعماله، مهامه، ديونه، ملاحظاته، وقراراته. لا تخترعي معلومات، ولا تطلبي بيانات حساسة بلا ضرورة. اجعلي الردود مختصرة وعملية وواعية. نادِ المستخدم باسم هشام عندما يكون مناسبًا.`;export function safeJsonParse(t){try{return JSON.parse(t)}catch{return{raw:t}}}export function getGeminiKeyInfo(){for(const name of["GEMINI_API_KEY","GEMINI_KEY","GOOGLE_API_KEY","GOOGLE_AI_API_KEY","GOOGLE_GENERATIVE_AI_API_KEY","GENERATIVE_LANGUAGE_API_KEY"]){const v=process.env[name];if(typeof v==="string"&&v.trim().length>0)return{key:v.trim(),source:name,length:v.trim().length}}return{key:"",source:null,length:0}}export function getGeminiModels(){return[process.env.GEMINI_MODEL,"gemini-2.5-flash","gemini-flash-latest","gemini-2.5-flash-lite"].filter(Boolean).map(m=>m.trim()).filter(Boolean)}export function getSafeEnvReport(){return Object.keys(process.env).filter(k=>{const u=k.toUpperCase();return u.includes("GEMINI")||u.includes("GOOGLE")||u.includes("API")||u.includes("KEY")||u.includes("LIA")}).sort()}export function getGeminiErrorMessage(d,s){return d?.error?.message||d?.error?.status||d?.message||d?.raw||`Gemini returned HTTP ${s}`}export async function callGemini({apiKey,prompt}){const attempts=[];for(const model of getGeminiModels()){const url=`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${apiKey}`;try{const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{temperature:.72,topP:.9,maxOutputTokens:1100}})});const text=await response.text();const data=safeJsonParse(text);if(!response.ok){attempts.push({model,status:response.status,error:getGeminiErrorMessage(data,response.status)});continue}const reply=data?.candidates?.[0]?.content?.parts?.[0]?.text||"يا هشام، وصلني الطلب لكن لم أستطع استخراج رد واضح من Gemini.";return{ok:true,model,reply}}catch(e){attempts.push({model,status:"FETCH_FAILED",error:e.message})}}return{ok:false,attempts}}